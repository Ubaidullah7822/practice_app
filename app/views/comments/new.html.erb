
<h2>Add a comment</h2>
<%= form_with(model: [@post, @comment],
              local: true) do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>
  <% end %>
